# MouseTrail 主题文件格式

*[English version](THEME-FORMAT.md)*

本文是 MouseTrail 导出轨迹样式时生成的 `.json` 文件的速查表，也用于你手动编写或
编辑自己的主题文件。在 **设置 → 轨迹 → 导入…** 中导入主题。

## 文件结构

一个主题文件就是一个 JSON 对象：

```json
{
  "version": 1,
  "appVersion": "1.1",
  "createdAt": "2026-07-20T12:00:00Z",
  "themes": [
    {
      "name": "my-color",
      "style": {
        "colorMode": { "fixed": { "red": 0.35, "green": 0.55, "blue": 0.28, "alpha": 1 } },
        "lifetime": 0.6,
        "widthScale": 1
      }
    }
  ]
}
```

| 字段 | 是否必需 | 说明 |
|---|---|---|
| `version` | 是 | 如果所有主题都使用 `rainbow`（彩虹）或 `fixed`（固定颜色），填 `1`；只要有任意主题使用 `gradient`（渐变），必须填 `2`。如果文件的 `version` 比当前 MouseTrail 能识别的版本更新，会被拒绝导入，并提示"此主题文件由更新版本的 MouseTrail 创建，请更新 MouseTrail 后再导入。"——不会出现"部分导入"的情况。 |
| `themes` | 是 | 主题对象数组（见下文）。可以只有一个主题，也可以有多个。 |
| `appVersion` | 否 | 仅作参考信息，记录导出该文件的应用版本号。手写文件可以省略。 |
| `createdAt` | 否 | 仅作参考信息，ISO 8601 时间戳。可以省略。 |

本文未列出的其他字段在导入时会被忽略，因此你可以放心地添加自己的备注作为额外的
顶层或主题内字段——MouseTrail 会跳过它不认识的内容。

## 主题（themes）

`themes` 数组中的每一项是：

```json
{
  "name": "my-color",
  "style": { "...": "见下文" }
}
```

- **`name`**——必须在你的 MouseTrail 预设列表中保持唯一。如果导入的主题名称与
  你已有的预设重名，MouseTrail 会保留两者：导入的主题会被自动重命名为
  "Name 2"（依次类推 "Name 3" …），而不会覆盖你已有的预设。
- **`style`**——轨迹的外观，包含 `colorMode`（颜色模式）、`lifetime`（持续时间）
  和 `widthScale`（宽度倍率），详见下文。

## 颜色模式

`style.colorMode` 是以下三种形式之一。应当只出现一个键（`rainbow`、`fixed`
或 `gradient`）——如果 MouseTrail 无法识别其形状（未知的键、缺少键，或
`gradient` 中没有可用的色标），会回退为 `rainbow`，而不会拒绝导入文件。

### `rainbow`——色相随鼠标移动循环变化

```json
{ "colorMode": { "rainbow": {} } }
```

没有其他字段。这是 MouseTrail 的经典外观。

### `fixed`——单一固定颜色

```json
{ "colorMode": { "fixed": { "red": 0.0, "green": 0.47, "blue": 0.75, "alpha": 1.0 } } }
```

| 字段 | 取值范围 | 含义 |
|---|---|---|
| `red`、`green`、`blue` | `0`–`1` | sRGB 颜色分量。 |
| `alpha` | `0`–`1` | 不透明度——`1` 为完全不透明，数值越小轨迹越透明。 |

### `gradient`——轨迹在 2–5 个色标之间渐变

```json
{
  "colorMode": {
    "gradient": {
      "stops": [
        { "red": 0.4824, "green": 0.3804, "blue": 1.0000, "alpha": 1, "location": 0 },
        { "red": 0.0000, "green": 0.8510, "blue": 1.0000, "alpha": 1, "location": 0.3333 },
        { "red": 0.0000, "green": 0.9608, "blue": 0.6275, "alpha": 1, "location": 0.6667 },
        { "red": 0.6588, "green": 1.0000, "blue": 0.4706, "alpha": 1, "location": 1 }
      ]
    }
  }
}
```

这就是 MouseTrail 内置的 **极光流光（Aurora）** 预设的完整写法——这些数值的
来源见下方"实例讲解"部分。

每个色标（stop）包含：

| 字段 | 取值范围 | 含义 |
|---|---|---|
| `red`、`green`、`blue`、`alpha` | `0`–`1` | 与 `fixed` 中的含义相同。 |
| `location` | `0`–`1` | 沿轨迹的位置：`0` 是**尾部**（最旧、正在淡出的一端），`1` 是**头部**（最新，紧邻鼠标指针）。 |

关于 `stops` 的规则：

- **2 到 5 个色标。** 少于 2 个或多于 5 个也不会被拒绝——MouseTrail 会对给定的
  内容做归一化处理（单个色标会被复制成两个相同颜色的色标，构成一个"纯色渐变"；
  超过 5 个色标只保留前 5 个）——但建议直接写 2–5 个，以得到你真正想要的渐变
  效果。
- 导入时会按 `location` 自动排序，因此不必按顺序书写，但按从尾到头
  （`location` 递增）的顺序书写最便于阅读和编辑。
- 超出范围的分量或 `location` 值会被限制（clamp）到 `0`–`1` 之间，而不是被
  拒绝。

## 颜色数值：十六进制转 0–1

MouseTrail 将颜色分量存储为 `0` 到 `1` 之间的 sRGB 浮点数，而非十六进制。要转换
十六进制颜色 `#RRGGBB`，将每个字节除以 255 即可：

```
分量 = 十六进制字节 / 255
```

| 十六进制字节 | 十进制 | 分量 |
|---|---|---|
| `#00` | 0 | `0.0000` |
| `#33` | 51 | `0.2000` |
| `#40` | 64 | `0.2510` |
| `#7F` | 127 | `0.4980` |
| `#80` | 128 | `0.5020` |
| `#B3` | 179 | `0.7020` |
| `#CC` | 204 | `0.8000` |
| `#FF` | 255 | `1.0000` |

实例：`#7B61FF` → 红色分量 `0x7B`（123）→ `123 / 255 ≈ 0.4824`，绿色分量
`0x61`（97）→ `97 / 255 ≈ 0.3804`，蓝色分量 `0xFF`（255）→
`255 / 255 = 1.0000`。

## 实例讲解：极光流光（Aurora）的渐变

MouseTrail 内置的 **极光流光（Aurora）** 预设使用以下四种颜色，在轨迹上均匀分布
（`location` 依次为 `0`、`1/3`、`2/3`、`1`）：

| 十六进制 | location | red | green | blue |
|---|---|---|---|---|
| `#7B61FF` | `0` | `0.4824` | `0.3804` | `1.0000` |
| `#00D9FF` | `0.3333` | `0.0000` | `0.8510` | `1.0000` |
| `#00F5A0` | `0.6667` | `0.0000` | `0.9608` | `0.6275` |
| `#A8FF78` | `1` | `0.6588` | `1.0000` | `0.4706` |

写成完整的主题文件（因为包含渐变，`version` 必须是 `2`）：

```json
{
  "version": 2,
  "themes": [
    {
      "name": "My Aurora Remix",
      "style": {
        "colorMode": {
          "gradient": {
            "stops": [
              { "red": 0.4824, "green": 0.3804, "blue": 1.0000, "alpha": 1, "location": 0 },
              { "red": 0.0000, "green": 0.8510, "blue": 1.0000, "alpha": 1, "location": 0.3333 },
              { "red": 0.0000, "green": 0.9608, "blue": 0.6275, "alpha": 1, "location": 0.6667 },
              { "red": 0.6588, "green": 1.0000, "blue": 0.4706, "alpha": 1, "location": 1 }
            ]
          }
        },
        "lifetime": 0.6,
        "widthScale": 1
      }
    }
  ]
}
```

## 持续时间与宽度

除了 `colorMode`，每个主题的 `style` 还有两个数值字段：

| 字段 | 取值范围 | 含义 |
|---|---|---|
| `lifetime` | `0.3`–`1.5`（秒） | 轨迹上某一点在淡出前保持可见的时长。数值越大，轨迹越长、停留越久。 |
| `widthScale` | `0.5`–`2.0` | 在 MouseTrail 7pt 基础线宽的基础上做倍率缩放。`1` 为默认宽度，`2` 为两倍宽，`0.5` 为一半宽。 |

超出这些范围的数值会被**限制到最接近的有效值，而不会被拒绝**——手写文件中
`lifetime` 填 `5`，导入后会变成 `1.5`，而不会报错。

## 使用技巧

- **均匀分布的渐变色标：** 对于 *N* 个色标，`location` 依次取
  `0, 1/(N-1), 2/(N-1), …, 1`。例如 4 个色标（如极光流光）对应
  `0, 0.3333, 0.6667, 1`。
- **半透明轨迹：** 降低 `fixed` 颜色或渐变各色标的 `alpha`（例如 `0.5`），可以
  得到更柔和、更透明的轨迹。不同色标可以设置不同的 `alpha`，因此渐变也可以在
  颜色变化的同时产生透明度的渐变。
- **导入方式：**
  - 在 **设置 → 轨迹 → 导入…** 中选择 `.json` 主题文件（旧版
    `.mousetrailpack` 文件同样支持导入）。
  - 来自本主题库展示页面的 `mousetrail://` 链接会自动触发相同的导入
    流程——MouseTrail 会先下载并请你确认，再添加该主题。
  - 导入不会覆盖同名的现有预设——名称冲突时，导入的主题会被自动重命名
    （如 "Name 2"）。
